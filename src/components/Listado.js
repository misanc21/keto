import React, {Fragment} from 'react'

const Listado = ({ lista }) => {
    return (
        <Fragment>
            <h2>Iteraciones</h2>
            <table>

            </table>
        </Fragment>
    )
}

export default Listado